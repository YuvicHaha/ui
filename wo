local Config={
    VehicleEnterRadius=15,
    VehicleEnterDelay=0.2,
    CamaroSpawnWait=1.0,
    MaxCarReturnDistance=200,
    CarFlyHeight=500,
    CarAscendSpeed=150,
    CarCruiseSpeed=400,
    CarCruiseDistanceThreshold=100,
    CarDescendSpeed=350,
    CarDescendDistanceThreshold=20,
    CarMaxRamDuration=0.1,
    CarLoopTpDuration=0.1,
    PlayerFlySpeed=120,
    PlayerReturnFlyHeight=60,
    Phase1VehicleSearchRadius=200,
    Phase1FinalWait=2,
    Phase1FinalRadius=25,
    VehicleAboveHeight=20,
    PlayerReturnCarDistance=100,
    AntiStuckTimeout=4,
    AntiStuckMinMove=5,
    PlayerFlyStopDistance=10,
    UndergroundYThreshold=15,
    CeilingCheckRayDistance=200,
    ArrestTpDistance=10,
    TpOffset=CFrame.new(0,0,5),
    WaypointDelay=0.2,
    ServerHopMinPlayers=25,
    ServerHopMaxPlayers=28,
    ServerHopCriminalLimit=2,
    AntiFullServerPlayers=29,
}
local Players=game:GetService("Players")
local RunService=game:GetService("RunService")
local ReplicatedStorage=game:GetService("ReplicatedStorage")
local UserInputService=game:GetService("UserInputService")
local VirtualInputManager=game:GetService("VirtualInputManager")
local TeleportService=game:GetService("TeleportService")
local HttpService=game:GetService("HttpService")
local LocalPlayer=Players.LocalPlayer
loadstring(game:HttpGet("https://raw.githubusercontent.com/YuvicHaha/ui3/refs/heads/main/jail"))()
getgenv().AutoArrestActive=false
getgenv().LagbackCount=0
local currentTarget=nil
local targetRoot=nil
local renderConnection=nil
local arrestedBlacklist={}
local ExecuteAutoArrest=nil
local ServerHopActive=false
local phase1Complete=false
local phase1PathComplete=false
local phase1FinalPosition=nil
local lastCarExited=nil
local resetQueued=false
local function setupPlayerTracking(plr)
    plr:GetPropertyChangedSignal("Team"):Connect(function()
        if plr.Team and plr.Team.Name~="Criminal" then
            arrestedBlacklist[plr]=nil
        end
    end)
end
for _,plr in pairs(Players:GetPlayers()) do
    if plr~=LocalPlayer then
        setupPlayerTracking(plr)
    end
end
Players.PlayerAdded:Connect(setupPlayerTracking)
Players.PlayerRemoving:Connect(function(plr)
    arrestedBlacklist[plr]=nil
end)
local function GetRequest()
    if request then
        return request
    end
    if http_request then
        return http_request
    end
    if syn and syn.request then
        return syn.request
    end
    return nil
end
local function GetCriminalCount()
    local count=0
    for _,plr in pairs(Players:GetPlayers()) do
        if plr.Team and plr.Team.Name=="Criminal" then
            count=count+1
        end
    end
    return count
end
local function GetServerList()
    local req=GetRequest()
    if not req then
        warn("[SERVER HOP] No HTTP request function available.")
        return nil
    end
    local cursor=""
    local servers={}
    for _=1,3 do
        local url="https://games.roblox.com/v1/games/"..game.PlaceId.."/servers/Public?sortOrder=Asc&limit=100"
        if cursor~="" then
            url=url.."&cursor="..HttpService:UrlEncode(cursor)
        end
        local ok,res=pcall(function()
            return req({
                Url=url,
                Method="GET",
                Headers={
                    ["Content-Type"]="application/json"
                }
            })
        end)
        if not ok or not res or not res.Body then
            warn("[SERVER HOP] Failed to get server list.")
            return nil
        end
        local decodeOk,data=pcall(function()
            return HttpService:JSONDecode(res.Body)
        end)
        if not decodeOk or not data or not data.data then
            warn("[SERVER HOP] Invalid server response.")
            return nil
        end
        for _,server in pairs(data.data) do
            if server.id and server.id~=game.JobId and server.playing and server.maxPlayers then
                table.insert(servers,server)
            end
        end
        cursor=data.nextPageCursor
        if not cursor or cursor=="" then
            break
        end
    end
    return servers
end
local function ServerHop(reason)
    if ServerHopActive then
        return
    end
    ServerHopActive=true
    getgenv().AutoArrestActive=false
    print("[SERVER HOP]",reason)
    if renderConnection then
        renderConnection:Disconnect()
        renderConnection=nil
    end
    local servers=GetServerList()
    if servers then
        local target=nil
        for _,server in pairs(servers) do
            if server.playing>=Config.ServerHopMinPlayers and server.playing<=Config.ServerHopMaxPlayers and server.playing<server.maxPlayers then
                target=server
                break
            end
        end
        if target then
            print("[SERVER HOP] Joining server:",target.playing.."/"..target.maxPlayers)
            local options=Instance.new("TeleportOptions")
            options.ServerInstanceId=target.id
            local ok,err=pcall(function()
                TeleportService:TeleportAsync(game.PlaceId,{LocalPlayer},options)
            end)
            if ok then
                return
            end
            warn("[SERVER HOP] Teleport failed:",err)
        end
    end
    ServerHopActive=false
    print("[SERVER HOP] No suitable server found, using normal teleport.")
    pcall(function()
        TeleportService:Teleport(game.PlaceId,LocalPlayer)
    end)
end
local function CheckServerHop()
    local playerCount=#Players:GetPlayers()
    local criminalCount=GetCriminalCount()
    print("[SERVER CHECK] Players:",playerCount,"Criminals:",criminalCount)
    if playerCount>=Config.AntiFullServerPlayers then
        ServerHop("ANTI-FULL SERVER")
        return true
    end
    if playerCount>=Config.ServerHopMinPlayers and playerCount<=Config.ServerHopMaxPlayers and criminalCount<Config.ServerHopCriminalLimit then
        ServerHop("TOO FEW CRIMINALS")
        return true
    end
    return false
end
Players.PlayerRemoving:Connect(function()
    task.defer(function()
        if getgenv().AutoArrestActive and not ServerHopActive then
            CheckServerHop()
        end
    end)
end)
Players.PlayerAdded:Connect(function()
    task.defer(function()
        if getgenv().AutoArrestActive and not ServerHopActive then
            CheckServerHop()
        end
    end)
end)
if not LPH_OBFUSCATED then
    LPH_JIT_MAX=function(...) return(...) end
    LPH_NO_VIRTUALIZE=function(...) return(...) end
    LRM_IsUserPremium=true
    script_key="a"
end
local Modules={
    TagUtils=require(ReplicatedStorage.Tag.TagUtils)
}
local OverwriteCnt=0
local ExitFunc=nil
if LPH_NO_VIRTUALIZE then
    LPH_NO_VIRTUALIZE(function()
        for i,v in pairs(getgc(true)) do
            if typeof(v)=="function" then
                if debug.info(v,"n"):match("CheatCheck") then
                    hookfunction(v,function() end)
                end
            end
            if typeof(v)=="function" and getfenv(v).script==LocalPlayer.PlayerScripts:FindFirstChild("LocalScript") then
                local con=getconstants(v)
                if table.find(con,"LastVehicleExit") and table.find(con,"tick") then
                    ExitFunc=v
                end
            end
        end
    end)()
    LPH_NO_VIRTUALIZE(function()
        for i,v in pairs(workspace:GetChildren()) do
            if v.Name=="Bench" then
                v:Destroy()
            end
        end
    end)()
    local TagUtils=require(ReplicatedStorage:FindFirstChild("TagUtils",true))
    if TagUtils then
        TagUtils.isPointInTag=LPH_NO_VIRTUALIZE(function(_,Tag)
            if Tag=="NoFallDamage" or Tag=="NoRagdoll" or Tag=="NoParachute" then
                return true
            end
        end)
    end
end
local vehicleUtils=require(ReplicatedStorage:WaitForChild("Vehicle"):WaitForChild("VehicleUtils"))
local rememberedCar
local function IsDriverVehicle(car,char,humanoid)
    if not car or not car.Parent or not char or not humanoid then
        return false
    end
    local seat=car:FindFirstChild("Seat") or car:FindFirstChild("VehicleSeat")
    if not seat then
        seat=car:FindFirstChildWhichIsA("VehicleSeat",true)
    end
    if not seat then
        return false
    end
    if seat.Occupant==humanoid then
        return true
    end
    local driver=car:FindFirstChild("Driver",true)
    if driver and driver:IsA("ObjectValue") and driver.Value==LocalPlayer then
        return true
    end
    return false
end
task.spawn(function()
    while task.wait(0.5) do
        local char=LocalPlayer.Character
        local humanoid=char and char:FindFirstChildOfClass("Humanoid")
        local ok,m=pcall(vehicleUtils.GetLocalVehicleModel)
        if ok and m and m~="" and m~=false and char and humanoid then
            local car=typeof(m)=="Instance" and m or workspace:FindFirstChild(m,true)
            if car and IsDriverVehicle(car,char,humanoid) and rememberedCar~=car then
                rememberedCar=car
                print("[SAVED DRIVER CAR]",car:GetFullName())
            end
        end
    end
end)
getgenv().GetMyCar=function()
    return rememberedCar
end
local function ResetCharacter(reason)
    if resetQueued then
        return true
    end
    resetQueued=true
    print("[RESET]",reason or "restart")
    getgenv().AutoArrestActive=false
    phase1Complete=false
    phase1PathComplete=false
    phase1FinalPosition=nil
    currentTarget=nil
    targetRoot=nil
    if renderConnection then
        renderConnection:Disconnect()
        renderConnection=nil
    end
    local char=LocalPlayer.Character
    local root=char and char:FindFirstChild("HumanoidRootPart")
    if root then
        pcall(function()
            root.AssemblyLinearVelocity=Vector3.zero
            root.AssemblyAngularVelocity=Vector3.zero
        end)
    end
    task.wait(0.1)
    pcall(function() LocalPlayer:LoadCharacter() end)
    return true
end
local function CheckLagback(rootPart)
    if not rootPart or not rootPart.Parent then
        return false
    end
    local lagged=false
    pcall(function()
        if type(LagBackCheck)=="function" then
            lagged=LagBackCheck(rootPart)
        end
    end)
    if lagged then
        getgenv().LagbackCount=getgenv().LagbackCount+1
        print("[LAGBACK]",getgenv().LagbackCount.."/20")
        if getgenv().LagbackCount>=20 then
            getgenv().LagbackCount=0
            ResetCharacter("20 lagbacks")
            return true
        end
    end
    return false
end
local function IsInVehicle(char,humanoid)
    if not char or not humanoid then
        return false
    end
    return char:GetAttribute("InVehicle")==true or humanoid.SeatPart~=nil
end
local function GetVehicleSeat(car)
    if not car or not car.Parent then
        return nil
    end
    local seat=car:FindFirstChild("Seat") or car:FindFirstChild("VehicleSeat")
    if seat and seat:IsA("Seat") or seat and seat:IsA("VehicleSeat") then
        return seat
    end
    return nil
end
local function IsVehicleAvailable(car)
    if not car or not car.Parent then
        return false
    end
    local seat=GetVehicleSeat(car)
    if not seat or seat.Occupant then
        return false
    end
    local driver=car:FindFirstChild("Driver")
    if not driver then
        driver=car:FindFirstChild("Driver",true)
    end
    if driver and driver:IsA("ObjectValue") and driver.Value then
        return false
    end
    return true
end
local function FindNearestAvailableVehicle(rootPart,radius)
    local folder=workspace:FindFirstChild("Vehicles")
    if not folder or not rootPart then
        return nil,nil
    end
    local best,bestDist=nil,math.huge
    local maxRadius=radius or Config.Phase1VehicleSearchRadius
    for _,v in ipairs(folder:GetChildren()) do
        local seat=v:FindFirstChild("Seat") or v:FindFirstChild("VehicleSeat")
        if seat and (seat:IsA("Seat") or seat:IsA("VehicleSeat")) and not seat.Occupant then
            local driver=v:FindFirstChild("Driver")
            if not driver then
                driver=v:FindFirstChild("Driver",true)
            end
            if not driver or not driver:IsA("ObjectValue") or not driver.Value then
                local dist=(seat.Position-rootPart.Position).Magnitude
                if dist<=maxRadius and dist<bestDist then
                    best=v
                    bestDist=dist
                end
            end
        end
    end
    return best,bestDist
end
local function IsObstacleAbove(rootPart,distance,vehicle)
    if not rootPart or not rootPart.Parent then
        return true
    end
    local rayParams=RaycastParams.new()
    rayParams.FilterType=Enum.RaycastFilterType.Exclude
    local ignoreList={LocalPlayer.Character}
    if vehicle and vehicle.Parent then
        table.insert(ignoreList,vehicle)
    end
    rayParams.FilterDescendantsInstances=ignoreList
    rayParams.IgnoreWater=true
    local result=workspace:Raycast(
        rootPart.Position,
        Vector3.new(0,distance or Config.CeilingCheckRayDistance,0),
        rayParams
    )
    if result and result.Instance and result.Instance.CanCollide then
        return true
    end
    return false
end
local function ExitAccidentalVehicle(humanoid)
    if not humanoid then
        return false
    end
    local inVehicle=humanoid.SeatPart~=nil
    local char=LocalPlayer.Character
    if char and char:GetAttribute("InVehicle") then
        inVehicle=true
    end
    if not inVehicle then
        return false
    end
    print("[ARREST SAFETY] Accidentally entered vehicle, exiting...")
    pcall(function()
        VirtualInputManager:SendKeyEvent(true,Enum.KeyCode.Space,false,game)
        task.wait(0.1)
        VirtualInputManager:SendKeyEvent(false,Enum.KeyCode.Space,false,game)
    end)
    task.wait(0.2)
    pcall(function()
        humanoid.Sit=false
    end)
    return true
end
local function EnterVehicleAsDriver(carModel)
    if not carModel or not carModel.Parent then
        return false
    end
    local uiFolder=ReplicatedStorage:FindFirstChild("Module")
    local uiObj=uiFolder and uiFolder:FindFirstChild("UI")
    local uiModule=nil
    pcall(function()
        if uiObj then
            uiModule=require(uiObj)
        elseif uiFolder then
            uiModule=require(uiFolder)
        end
    end)
    if not uiModule or not uiModule.CircleAction or not uiModule.CircleAction.Specs then
        return false
    end
    local driverSeat=GetVehicleSeat(carModel)
    local chosen=nil
    for _,spec in pairs(uiModule.CircleAction.Specs) do
        local part=spec.Part
        if part and part:IsDescendantOf(carModel) then
            if driverSeat and part==driverSeat then
                chosen=spec
                break
            end
            local text=tostring(spec.Text or ""):lower()
            local partName=part.Name:lower()
            if (text:find("driver") or text:find("drive")) and not text:find("passenger") and not text:find("eject") then
                chosen=spec
                break
            end
            if not chosen and not text:find("passenger") and not text:find("eject") and not partName:find("passenger") then
                chosen=spec
            end
        end
    end
    if not chosen or type(chosen.Callback)~="function" then
        return false
    end
    task.spawn(function()
        pcall(function()
            chosen:Callback(true)
        end)
    end)
    return true
end
local function WaitForSeated(char,humanoid,timeout)
    local start=tick()
    local limit=timeout or 2.0
    while tick()-start<limit do
        if not char or not char.Parent or not humanoid or not humanoid.Parent then
            return false
        end
        if char:GetAttribute("InVehicle") or humanoid.SeatPart~=nil then
            return true
        end
        task.wait(0.05)
    end
    return false
end
local function GoToClosestStationAndGetCar(rootPart,char,humanoid)
    if not rootPart or not char or not humanoid then
        return false
    end
    local PoliceData={
        ["Custom Main Station Path"]={
            Base=Vector3.new(-1173.24,39.42,-1583.77),
            Path={
                Vector3.new(-1173.24,39.42,-1583.77),
                Vector3.new(-1177.93,39.42,-1580.66),
                Vector3.new(-1184.32,39.42,-1575.63),
                Vector3.new(-1188.49,39.42,-1568.75),
                Vector3.new(-1187.33,39.42,-1560.60),
                Vector3.new(-1184.78,39.42,-1552.89),
                Vector3.new(-1182.62,39.42,-1546.72),
                Vector3.new(-1175.61,34.97,-1546.03),
                Vector3.new(-1168.76,30.16,-1546.55),
                Vector3.new(-1161.44,27.15,-1547.99),
                Vector3.new(-1156.07,23.25,-1551.89),
                Vector3.new(-1154.36,21.12,-1559.06),
                Vector3.new(-1157.26,19.07,-1566.29),
                Vector3.new(-1162.58,19.02,-1572.38),
                Vector3.new(-1169.74,19.02,-1576.19),
                Vector3.new(-1177.62,19.02,-1578.67),
                Vector3.new(-1185.61,19.07,-1580.14),
                Vector3.new(-1193.86,19.07,-1580.63),
                Vector3.new(-1201.85,19.07,-1580.43),
                Vector3.new(-1209.98,19.07,-1580.09),
                Vector3.new(-1218.11,19.07,-1580.18),
                Vector3.new(-1226.36,19.07,-1579.78),
                Vector3.new(-1234.43,19.02,-1578.75),
                Vector3.new(-1239.75,19.07,-1573.05),
                Vector3.new(-1243.11,19.07,-1565.64),
                Vector3.new(-1248.50,19.02,-1559.53),
                Vector3.new(-1256.50,19.02,-1558.65),
                Vector3.new(-1262.43,19.02,-1554.62),
                Vector3.new(-1259.15,18.71,-1549.22),
                Vector3.new(-1251.26,18.62,-1547.54),
                Vector3.new(-1244.88,18.62,-1546.87)
            }
        },
        ["Fourth Station"]={
            Base=Vector3.new(1786.66,24.05,-4019.01),
            Path={
                Vector3.new(1762.26,24.00,-4024.93),
                Vector3.new(1765.20,24.02,-4026.29),
                Vector3.new(1772.61,24.05,-4029.64),
                Vector3.new(1779.17,24.00,-4034.16),
                Vector3.new(1785.04,24.00,-4040.16),
                Vector3.new(1790.33,24.25,-4046.51),
                Vector3.new(1792.76,24.50,-4049.68),
                Vector3.new(1793.25,24.50,-4053.24),
                Vector3.new(1790.91,24.09,-4061.18),
                Vector3.new(1786.38,24.10,-4068.25),
                Vector3.new(1784.77,24.10,-4076.59),
                Vector3.new(1781.65,23.80,-4084.53),
                Vector3.new(1778.85,23.80,-4092.15),
                Vector3.new(1778.73,23.80,-4093.07)
            }
        },
        ["Main Police Station Ground"]={
            Base=Vector3.new(-1171.10,18.80,-1579.22),
            Path={
                Vector3.new(-1142.16,18.85,-1586.02),
                Vector3.new(-1146.80,18.85,-1585.55),
                Vector3.new(-1155.16,18.85,-1584.71),
                Vector3.new(-1163.53,18.85,-1583.90),
                Vector3.new(-1171.65,18.85,-1583.56),
                Vector3.new(-1179.64,18.85,-1583.26),
                Vector3.new(-1188.04,18.85,-1582.97),
                Vector3.new(-1196.30,18.85,-1582.68),
                Vector3.new(-1204.70,18.85,-1582.39),
                Vector3.new(-1212.83,18.85,-1582.11),
                Vector3.new(-1220.96,18.85,-1581.82),
                Vector3.new(-1229.09,18.85,-1581.42),
                Vector3.new(-1236.90,18.80,-1579.45),
                Vector3.new(-1241.89,18.85,-1573.22),
                Vector3.new(-1244.30,18.85,-1565.59),
                Vector3.new(-1249.61,18.80,-1559.48),
                Vector3.new(-1257.60,18.80,-1559.35),
                Vector3.new(-1263.00,18.79,-1555.37),
                Vector3.new(-1260.26,18.40,-1547.61),
                Vector3.new(-1252.41,18.40,-1546.17),
                Vector3.new(-1244.15,18.40,-1546.12),
                Vector3.new(-1244.08,18.40,-1546.12)
            }
        },
        ["Museum Police Station"]={
            Base=Vector3.new(738.20,44.96,1120.53),
            Path={
                Vector3.new(732.75,44.96,1125.10),
                Vector3.new(733.10,44.96,1122.05),
                Vector3.new(733.02,44.96,1113.69),
                Vector3.new(731.74,44.96,1105.80),
                Vector3.new(730.05,44.96,1097.57),
                Vector3.new(728.25,44.95,1089.37),
                Vector3.new(730.33,44.94,1081.29),
                Vector3.new(735.58,44.37,1075.00),
                Vector3.new(741.75,44.77,1069.30),
                Vector3.new(744.70,44.96,1066.60)
            }
        },
        ["Military Base"]={
            Base=Vector3.new(1801.00,25.54,-891.02),
            Path={
                Vector3.new(1805.17,25.54,-900.65),
                Vector3.new(1805.17,25.54,-900.65),
                Vector3.new(1799.38,25.54,-901.80),
                Vector3.new(1791.31,25.57,-902.75),
                Vector3.new(1783.10,25.60,-903.71),
                Vector3.new(1775.02,25.54,-904.70),
                Vector3.new(1766.97,25.54,-905.80),
                Vector3.new(1758.94,25.54,-907.13),
                Vector3.new(1753.12,25.54,-908.27),
                Vector3.new(1753.12,25.54,-908.27)
            }
        },
        ["Fifth Station"]={
            Base=Vector3.new(1822.10,25.55,-636.60),
            Path={
                Vector3.new(1820.55,25.54,-607.59),
                Vector3.new(1822.75,25.54,-616.20),
                Vector3.new(1827.02,25.54,-623.28),
                Vector3.new(1831.25,25.54,-630.38),
                Vector3.new(1835.55,25.08,-637.60),
                Vector3.new(1844.01,25.00,-651.80),
                Vector3.new(1848.31,25.00,-659.02)
            }
        }
    }
    local closestStation=nil
    local minBaseDist=math.huge
    for _,data in pairs(PoliceData) do
        local dist=(rootPart.Position-data.Base).Magnitude
        if dist<minBaseDist then
            minBaseDist=dist
            closestStation=data
        end
    end
    if not phase1PathComplete then
        if closestStation then
            for _,waypoint in ipairs(closestStation.Path) do
                if not getgenv().AutoArrestActive then
                    return false
                end
                rootPart.CFrame=CFrame.new(waypoint)
                if CheckLagback(rootPart) then
                    return false
                end
                task.wait(Config.WaypointDelay)
            end
        end
        phase1PathComplete=true
        phase1FinalPosition=rootPart.Position
        print("[PHASE 1] Destination reached. Vehicle search unlocked.")
    end
    if not phase1FinalPosition then
        phase1FinalPosition=rootPart.Position
    end
    local waitStart=tick()
    while tick()-waitStart<Config.Phase1FinalWait and getgenv().AutoArrestActive do
        if (rootPart.Position-phase1FinalPosition).Magnitude>Config.Phase1FinalRadius then
            ResetCharacter("lagback away from Phase 1 destination")
            return false
        end
        task.wait(0.1)
    end
    if not getgenv().AutoArrestActive then
        return false
    end
    if IsInVehicle(char,humanoid) then
        phase1Complete=true
        return true
    end
    local car=FindNearestAvailableVehicle(rootPart,Config.Phase1VehicleSearchRadius)
    if not car then
        print("[PHASE 1] No free vehicle within 200 studs. Trying Camaro spawn...")
        local spawnRemote=ReplicatedStorage:FindFirstChild("GarageSpawnVehicle",true)
        if spawnRemote then
            pcall(function() spawnRemote:FireServer("Chassis","Camaro") end)
            task.wait(Config.CamaroSpawnWait)
            car=FindNearestAvailableVehicle(rootPart,Config.Phase1VehicleSearchRadius)
        end
    end
    if not car then
        print("[PHASE 1] No available vehicle within 200 studs. Waiting before checking again...")
        task.wait(2)
        return false
    end
    local seat=GetVehicleSeat(car)
    if not seat then
        return false
    end
    print("[PHASE 1] Found free vehicle:",car.Name)
    local above=seat.Position+Vector3.new(0,Config.VehicleAboveHeight,0)
    FlyPlayerTo(rootPart,above,8,Config.PlayerFlySpeed,false)
    if not getgenv().AutoArrestActive or not rootPart.Parent then
        return false
    end
    FlyPlayerTo(rootPart,seat.Position+Vector3.new(0,3,0),10,Config.PlayerFlySpeed,false)
    if not getgenv().AutoArrestActive then
        return false
    end
    if (rootPart.Position-seat.Position).Magnitude<=15 and IsVehicleAvailable(car) then
        EnterVehicleAsDriver(car)
        if WaitForSeated(char,humanoid,1.5) then
            phase1Complete=true
            return true
        end
    end
    return false
end
local function IsTargetCovered(root)
    if not root then
        return true
    end
    local rayParams=RaycastParams.new()
    rayParams.FilterType=Enum.RaycastFilterType.Exclude
    local ignoreList={LocalPlayer.Character,root.Parent}
    if workspace:FindFirstChild("Vehicles") then
        table.insert(ignoreList,workspace.Vehicles)
    end
    rayParams.FilterDescendantsInstances=ignoreList
    rayParams.IgnoreWater=true
    local rayResult=workspace:Raycast(
        root.Position,
        Vector3.new(0,Config.CeilingCheckRayDistance,0),
        rayParams
    )
    if rayResult and rayResult.Instance and rayResult.Instance.CanCollide then
        return true
    end
    return false
end
local function isTargetValid(root)
    if not root or not root.Parent then
        return false
    end
    local plr=Players:GetPlayerFromCharacter(root.Parent)
    if not plr or arrestedBlacklist[plr] then
        return false
    end
    if plr.Team and plr.Team.Name~="Criminal" then
        return false
    end
    local char=root.Parent
    if char:GetAttribute("HasHandcuffs") then
        return false
    end
    if root.Position.Y<Config.UndergroundYThreshold then
        return false
    end
    if IsTargetCovered(root) then
        return false
    end
    if char:GetAttribute("InVehicle") then
        local vehiclesDir=workspace:FindFirstChild("Vehicles")
        if vehiclesDir then
            for _,v in pairs(vehiclesDir:GetChildren()) do
                local seat=v:FindFirstChild("Seat") or v:FindFirstChild("VehicleSeat") or v:FindFirstChildWhichIsA("BasePart",true)
                if seat and (seat.Position-root.Position).Magnitude<15 then
                    local vName=v.Name:lower()
                    if vName:find("heli") or vName:find("drone") or vName:find("blimp") then
                        return false
                    end
                end
            end
        end
    end
    return true
end
local function getClosestCriminal(p_root)
    local bestRoot,bestDist=nil,math.huge
    local selectedPlr
    for _,plr in pairs(Players:GetPlayers()) do
        if plr~=LocalPlayer and plr.Character and not arrestedBlacklist[plr] then
            if plr.Team and plr.Team.Name=="Criminal" then
                local root=plr.Character:FindFirstChild("HumanoidRootPart")
                if isTargetValid(root) then
                    local d=(root.Position-p_root.Position).Magnitude
                    if d<bestDist then
                        bestDist=d
                        bestRoot=root
                        selectedPlr=plr
                    end
                end
            end
        end
    end
    return selectedPlr,bestRoot
end
local function ScanTerrainAhead(rootPart,checkDirection)
    if not rootPart or not rootPart.Parent then
        return rootPart and rootPart.Position.Y or 0,rootPart and rootPart.Position.Y or 0,false,0
    end
    local dirUnit=(checkDirection and checkDirection.Magnitude>0.1) and checkDirection.Unit or rootPart.CFrame.LookVector
    local origin=rootPart.Position
    local params=RaycastParams.new()
    params.FilterType=Enum.RaycastFilterType.Include
    params.FilterDescendantsInstances={workspace.Terrain}
    local groundRay=workspace:Raycast(origin+Vector3.new(0,10,0),Vector3.new(0,-2000,0),params)
    local groundY=groundRay and groundRay.Position.Y or origin.Y-5
    local distances={15,40,80,130,180,250}
    local highest=groundY
    local found=false
    local direct=workspace:Raycast(origin,dirUnit*250,params)
    local hitDist=direct and (direct.Position-origin).Magnitude or 250
    if direct then table.insert(distances,math.max(1,hitDist)) end
    local skyY=math.max(origin.Y+1500,2000)
    for _,dist in ipairs(distances) do
        local point=origin+dirUnit*dist
        local hit=workspace:Raycast(Vector3.new(point.X,skyY,point.Z),Vector3.new(0,-4000,0),params)
        if hit then
            highest=math.max(highest,hit.Position.Y)
            if (hit.Position.Y-groundY)>=20 or (hit.Position.Y-origin.Y)>=20 then
                found=true
            end
        end
    end
    return groundY,highest,found,hitDist
end
local function FlyPlayerTo(rootPart,targetPos,stopDist,speed,useTerrain)
    if not rootPart or not rootPart.Parent then
        return
    end
    if rootPart:FindFirstChild("ExecFlyAttachment") then rootPart.ExecFlyAttachment:Destroy() end
    if rootPart:FindFirstChild("ExecFlyVelocity") then rootPart.ExecFlyVelocity:Destroy() end
    local attachment=Instance.new("Attachment",rootPart)
    attachment.Name="ExecFlyAttachment"
    local linearVelocity=Instance.new("LinearVelocity",rootPart)
    linearVelocity.Name="ExecFlyVelocity"
    linearVelocity.Attachment0=attachment
    linearVelocity.RelativeTo=Enum.ActuatorRelativeTo.World
    linearVelocity.MaxForce=math.huge
    linearVelocity.VectorVelocity=Vector3.zero
    linearVelocity.Enabled=true
    local lastPos=rootPart.Position
    local stuckSince=tick()
    while getgenv().AutoArrestActive and rootPart and rootPart.Parent do
        if CheckLagback(rootPart) then break end
        local moved=(rootPart.Position-lastPos).Magnitude
        if moved>=Config.AntiStuckMinMove then
            lastPos=rootPart.Position
            stuckSince=tick()
        elseif tick()-stuckSince>=Config.AntiStuckTimeout then
            ResetCharacter("anti-stuck during flight")
            break
        end
        local dist=(rootPart.Position-targetPos).Magnitude
        if dist<=(stopDist or 10) then break end
        local velocity=(targetPos-rootPart.Position).Unit*(speed or Config.PlayerFlySpeed)
        if useTerrain then
            local groundY,peakY,mountainFound,forwardDist=ScanTerrainAhead(rootPart,velocity)
            local targetHeight=groundY+5
            if mountainFound then targetHeight=peakY+20 end
            local yDiff=targetHeight-rootPart.Position.Y
            local vy=0
            if yDiff>1 then vy=math.clamp(yDiff*6,25,170) elseif yDiff<-1 then vy=-math.clamp(math.abs(yDiff)*4,20,350) end
            local x,z=velocity.X,velocity.Z
            if forwardDist<15 and yDiff>5 then x=x*0.3 z=z*0.3 end
            if Vector3.new(x,0,z).Magnitude<0.1 and yDiff>5 then
                local f=rootPart.CFrame.LookVector
                x=f.X*(speed or Config.PlayerFlySpeed)*0.4
                z=f.Z*(speed or Config.PlayerFlySpeed)*0.4
            end
            velocity=Vector3.new(x,vy,z)
        end
        linearVelocity.VectorVelocity=velocity
        task.wait()
    end
    if linearVelocity and linearVelocity.Parent then linearVelocity:Destroy() end
    if attachment and attachment.Parent then attachment:Destroy() end
end
local function EquipHandcuffs()
    local invSys=require(ReplicatedStorage.Inventory.InventoryItemSystem)
    for _,item in pairs(invSys.getInventoryItemsFor(LocalPlayer)) do
        if item.obj and item.obj.Name=="Handcuffs" then
            pcall(function()
                item:AttemptSetEquipped(true)
            end)
            return true
        end
    end
    return false
end
local function UnequipHandcuffs()
    local invSys=require(ReplicatedStorage.Inventory.InventoryItemSystem)
    for _,item in pairs(invSys.getInventoryItemsFor(LocalPlayer)) do
        if item.obj and item.obj.Name=="Handcuffs" then
            pcall(function()
                item:AttemptSetEquipped(false)
            end)
            return true
        end
    end
    return false
end
local function EjectNearestVehicleUser(targetRoot)
    local targetChar=targetRoot and targetRoot.Parent
    local targetVehicle=nil
    if targetChar and targetChar:GetAttribute("InVehicle") then
        local vehiclesDir=workspace:FindFirstChild("Vehicles")
        if vehiclesDir then
            for _,v in pairs(vehiclesDir:GetChildren()) do
                local seat=v:FindFirstChild("Seat") or v:FindFirstChild("VehicleSeat") or v:FindFirstChildWhichIsA("BasePart",true)
                if seat and (seat.Position-targetRoot.Position).Magnitude<15 then
                    targetVehicle=v
                    break
                end
            end
        end
    end
    if not targetVehicle then
        return
    end
    local uiModule=ReplicatedStorage:FindFirstChild("Module",true) and require(ReplicatedStorage.Module:FindFirstChild("UI",true) or ReplicatedStorage.Module)
    if uiModule and uiModule.CircleAction and uiModule.CircleAction.Specs then
        for _,spec in pairs(uiModule.CircleAction.Specs) do
            local isMatch=false
            if spec.Part and (targetVehicle:IsAncestorOf(spec.Part) or spec.Part:IsDescendantOf(targetVehicle)) then
                isMatch=true
            elseif spec.Text and (spec.Text:lower():find("eject") or spec.Text:lower():find("passenger")) then
                isMatch=true
            end
            if isMatch then
                pcall(function()
                    spec:Callback(true)
                end)
                return
            end
        end
    end
end
local attemptArrest
for _,v in pairs(getgc(true)) do
    if type(v)=="function" and islclosure(v) then
        pcall(function()
            if tostring(getinfo(v).name)=="AttemptArrest" then
                attemptArrest=v
            end
        end)
        if attemptArrest then
            break
        end
    end
end
local function ReturnToLastExitedCar(rootPart,char,humanoid)
    if not lastCarExited or not lastCarExited.Parent then
        return false
    end
    local seat=GetVehicleSeat(lastCarExited)
    if not seat then
        return false
    end
    if (rootPart.Position-seat.Position).Magnitude<=Config.PlayerReturnCarDistance then
        return false
    end
    print("[FOLLOW SAFETY] More than 100 studs from last car. Returning to it...")
    if renderConnection then renderConnection:Disconnect() renderConnection=nil end
    FlyPlayerTo(rootPart,seat.Position+Vector3.new(0,Config.VehicleAboveHeight,0),10,Config.PlayerFlySpeed,false)
    if not getgenv().AutoArrestActive then return true end
    FlyPlayerTo(rootPart,seat.Position+Vector3.new(0,3,0),12,Config.PlayerFlySpeed,false)
    if (rootPart.Position-seat.Position).Magnitude<=15 and IsVehicleAvailable(lastCarExited) then
        EnterVehicleAsDriver(lastCarExited)
        if WaitForSeated(char,humanoid,1.5) then
            print("[FOLLOW SAFETY] Re-entered last exited car.")
            task.wait(0.2)
            lastCarExited=nil
            return true
        end
    end
    return true
end
local function ExecuteAutoArrestCycle()
    if not getgenv().AutoArrestActive then
        return
    end
    if not phase1Complete then
        return
    end
    local char=LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
    local rootPart=char:WaitForChild("HumanoidRootPart")
    local humanoid=char:WaitForChild("Humanoid")
    local checkPlr,checkRoot=getClosestCriminal(rootPart)
    if not checkRoot then
        print("[STANDBY] No valid surface targets.")
        local inVehicleNow=IsInVehicle(char,humanoid)
        if inVehicleNow then
            local BodyGyro=Instance.new("BodyGyro",rootPart)
            local BodyVelocity=Instance.new("BodyVelocity",rootPart)
            BodyVelocity.MaxForce=Vector3.new(math.huge,math.huge,math.huge)
            BodyGyro.MaxTorque=Vector3.new(math.huge,math.huge,math.huge)
            BodyGyro.D=500
            BodyGyro.P=150000
            while getgenv().AutoArrestActive do
                if not IsInVehicle(char,humanoid) then
                    BodyVelocity:Destroy()
                    BodyGyro:Destroy()
                    ResetCharacter("dismounted during standby ascent")
                    return
                end
                local _,testRoot=getClosestCriminal(rootPart)
                if testRoot then
                    break
                end
                if CheckLagback(rootPart) then
                    BodyVelocity:Destroy()
                    BodyGyro:Destroy()
                    return
                end
                if rootPart.Position.Y<Config.CarFlyHeight then
                    if IsObstacleAbove(rootPart,Config.CeilingCheckRayDistance,humanoid.SeatPart and humanoid.SeatPart.Parent) then
                        BodyVelocity.Velocity=Vector3.zero
                        break
                    end
                    BodyVelocity.Velocity=Vector3.new(0,Config.CarAscendSpeed,0)
                    rootPart.CFrame=rootPart.CFrame+Vector3.new(0,10,0)
                else
                    BodyVelocity.Velocity=Vector3.zero
                end
                BodyGyro.CFrame=CFrame.new(rootPart.Position,rootPart.Position+Vector3.new(0,0,-1))
                task.wait(0.2)
            end
            BodyVelocity:Destroy()
            BodyGyro:Destroy()
        else
            task.wait(1)
        end
        return
    end
    local inVehicle=char:GetAttribute("InVehicle") or (humanoid.SeatPart~=nil)
    if not inVehicle then
        local myCar=getgenv().GetMyCar and getgenv().GetMyCar()
        local carValid=myCar and myCar:IsDescendantOf(workspace)
        local seat=carValid and (
            myCar:FindFirstChild("Seat") or
            myCar:FindFirstChild("VehicleSeat") or
            myCar:FindFirstChildWhichIsA("BasePart",true)
        )
        local carDist=seat and (rootPart.Position-seat.Position).Magnitude or math.huge
        if carValid and seat and carDist<=Config.MaxCarReturnDistance then
            print("[FLYING BACK TO SAVED CAR]")
            local returnHeight=rootPart.Position.Y+Config.PlayerReturnFlyHeight
            if not IsObstacleAbove(rootPart,Config.PlayerReturnFlyHeight,myCar) then
                FlyPlayerTo(rootPart,Vector3.new(rootPart.Position.X,returnHeight,rootPart.Position.Z),10,Config.PlayerFlySpeed,false)
            end
            if not getgenv().AutoArrestActive then
                return
            end
            local returnHeight=rootPart.Position.Y+Config.PlayerReturnFlyHeight
            FlyPlayerTo(
                rootPart,
                Vector3.new(seat.Position.X,returnHeight,seat.Position.Z),
                10,
                Config.PlayerFlySpeed
            )
            if not getgenv().AutoArrestActive then
                return
            end
            FlyPlayerTo(
                rootPart,
                seat.Position+Vector3.new(0,3,0),
                12,
                Config.PlayerFlySpeed
            )
            if (rootPart.Position-seat.Position).Magnitude<=15 then
                EnterVehicleAsDriver(myCar)
                inVehicle=WaitForSeated(char,humanoid,1.5)
            end
        else
            print("[SAVED CAR TOO FAR] Searching for nearest free vehicle within 200 studs...")
            local nearestCar,nearestDist=FindNearestAvailableVehicle(rootPart,Config.Phase1VehicleSearchRadius)
            if nearestCar then
                local seat=GetVehicleSeat(nearestCar)
                if seat then
                    print("[FOUND VEHICLE]",nearestCar.Name)
                    local returnHeight=math.max(rootPart.Position.Y+Config.PlayerReturnFlyHeight,seat.Position.Y+Config.VehicleAboveHeight)
                    FlyPlayerTo(rootPart,Vector3.new(seat.Position.X,returnHeight,seat.Position.Z),10,Config.PlayerFlySpeed,false)
                    FlyPlayerTo(rootPart,seat.Position+Vector3.new(0,3,0),12,Config.PlayerFlySpeed,false)
                    if (rootPart.Position-seat.Position).Magnitude<=15 and IsVehicleAvailable(nearestCar) then
                        EnterVehicleAsDriver(nearestCar)
                        inVehicle=WaitForSeated(char,humanoid,1.5)
                    end
                end
            end
        end
        if not inVehicle then
            print("[SPAWNING CAMARO - LAST RESORT]")
            local spawnRemote=ReplicatedStorage:FindFirstChild("GarageSpawnVehicle",true)
            if spawnRemote then
                spawnRemote:FireServer("Chassis","Camaro")
                task.wait(Config.CamaroSpawnWait)
                for _,v in pairs(workspace.Vehicles:GetChildren()) do
                    local vSeat=v:FindFirstChild("Seat") or v:FindFirstChild("VehicleSeat")
                    if vSeat and (vSeat.Position-rootPart.Position).Magnitude<Config.VehicleEnterRadius then
                        EnterVehicleAsDriver(v)
                        if WaitForSeated(char,humanoid,1.5) then
                            inVehicle=true
                            break
                        end
                    end
                end
            end
        end
    end
    if not inVehicle then
        task.wait(1)
        return
    end
    local doubleCheckVehicle=IsInVehicle(char,humanoid)
    if not doubleCheckVehicle then
        print("[SAFETY CHECK] Not in vehicle yet, skipping upward flight...")
        task.wait(1)
        return
    end
    task.wait(0.2)
    local BodyGyro=Instance.new("BodyGyro",rootPart)
    local BodyVelocity=Instance.new("BodyVelocity",rootPart)
    BodyVelocity.MaxForce=Vector3.new(math.huge,math.huge,math.huge)
    BodyGyro.MaxTorque=Vector3.new(math.huge,math.huge,math.huge)
    BodyGyro.D=500
    BodyGyro.P=150000
    BodyVelocity.Velocity=Vector3.new(0,Config.CarAscendSpeed,0)
    BodyGyro.CFrame=CFrame.new(rootPart.Position,rootPart.Position+Vector3.new(0,0,-1))
    local ascentBlocked=false
    repeat
        if CheckLagback(rootPart) then
            BodyVelocity:Destroy()
            BodyGyro:Destroy()
            return
        end
        if not IsInVehicle(char,humanoid) then
            BodyVelocity:Destroy()
            BodyGyro:Destroy()
            ResetCharacter("dismounted during vehicle ascent")
            return
        end
        if rootPart.Position.Y<Config.CarFlyHeight then
            if IsObstacleAbove(
                rootPart,
                Config.CeilingCheckRayDistance,
                humanoid.SeatPart and humanoid.SeatPart.Parent
            ) then
                BodyVelocity.Velocity=Vector3.zero
                print("[ASCEND BLOCKED] Obstacle detected above vehicle.")
                ascentBlocked=true
                break
            end
            rootPart.CFrame=rootPart.CFrame+Vector3.new(0,10,0)
        end
        task.wait(0.1)
    until rootPart.Position.Y>=Config.CarFlyHeight or not getgenv().AutoArrestActive
    if ascentBlocked then
        BodyVelocity:Destroy()
        BodyGyro:Destroy()
        task.wait(0.5)
        return
    end
    if not getgenv().AutoArrestActive or not IsInVehicle(char,humanoid) then
        BodyVelocity:Destroy()
        BodyGyro:Destroy()
        if not IsInVehicle(char,humanoid) then ResetCharacter("dismounted after ascent") end
        return
    end
    local targetPlayer,targetRoot
    repeat
        if CheckLagback(rootPart) then
            BodyVelocity:Destroy()
            BodyGyro:Destroy()
            return
        end
        targetPlayer,targetRoot=getClosestCriminal(rootPart)
        BodyVelocity.Velocity=Vector3.zero
        task.wait(0.1)
    until targetRoot or not getgenv().AutoArrestActive
    if not getgenv().AutoArrestActive then
        BodyVelocity:Destroy()
        BodyGyro:Destroy()
        return
    end
    while getgenv().AutoArrestActive and isTargetValid(targetRoot) do
        if not IsInVehicle(char,humanoid) then
            BodyVelocity:Destroy()
            BodyGyro:Destroy()
            ResetCharacter("dismounted while flying to target")
            return
        end
        if CheckLagback(rootPart) then
            BodyVelocity:Destroy()
            BodyGyro:Destroy()
            return
        end
        local targetPosXZ=Vector3.new(
            targetRoot.Position.X,
            Config.CarFlyHeight,
            targetRoot.Position.Z
        )
        local dist=(
            Vector3.new(
                rootPart.Position.X,
                Config.CarFlyHeight,
                rootPart.Position.Z
            )-targetPosXZ
        ).Magnitude
        if dist>Config.CarCruiseDistanceThreshold then
            BodyVelocity.Velocity=(targetPosXZ-rootPart.Position).Unit*Config.CarCruiseSpeed
            BodyGyro.CFrame=CFrame.lookAt(rootPart.Position,targetPosXZ)
        else
            break
        end
        task.wait()
    end
    while getgenv().AutoArrestActive and isTargetValid(targetRoot) do
        if not IsInVehicle(char,humanoid) then
            BodyVelocity:Destroy()
            BodyGyro:Destroy()
            ResetCharacter("dismounted while descending to target")
            return
        end
        if CheckLagback(rootPart) then
            BodyVelocity:Destroy()
            BodyGyro:Destroy()
            return
        end
        local targetPos=targetRoot.Position
        local dist=(rootPart.Position-targetPos).Magnitude
        if dist>Config.CarDescendDistanceThreshold then
            BodyVelocity.Velocity=(targetPos-rootPart.Position).Unit*Config.CarDescendSpeed
            BodyGyro.CFrame=CFrame.lookAt(rootPart.Position,targetPos)
        else
            break
        end
        task.wait()
    end
    if not isTargetValid(targetRoot) then
        BodyVelocity:Destroy()
        BodyGyro:Destroy()
        print("[TARGET LOST] Returning to standby...")
        return
    end
    if getgenv().AutoArrestActive and isTargetValid(targetRoot) then
        local targetChar=targetRoot.Parent
        local targetVehicle=nil
        if targetChar:GetAttribute("InVehicle") then
            local vehiclesDir=workspace:FindFirstChild("Vehicles")
            if vehiclesDir then
                for _,v in pairs(vehiclesDir:GetChildren()) do
                    local seat=v:FindFirstChild("Seat") or v:FindFirstChild("VehicleSeat") or v:FindFirstChildWhichIsA("BasePart",true)
                    if seat and (seat.Position-targetRoot.Position).Magnitude<15 then
                        targetVehicle=v
                        break
                    end
                end
            end
        end
        if not targetChar:GetAttribute("InVehicle") then
            print("[TARGET ON FOOT] Skipping tire pop and ramming phase...")
        elseif targetVehicle then
            local initialPop=targetVehicle:GetAttribute("VehicleTiresLastPop")
            local rStart=tick()
            local maxRamTime=math.max(Config.CarMaxRamDuration,0.8)
            while getgenv().AutoArrestActive and isTargetValid(targetRoot) and (tick()-rStart<maxRamTime) do
                if CheckLagback(rootPart) then
                    BodyVelocity:Destroy()
                    BodyGyro:Destroy()
                    return
                end
                pcall(function()
                    sethiddenproperty(rootPart,"PhysicsRepRootPart",targetRoot)
                end)
                rootPart.CFrame=targetRoot.CFrame*Config.TpOffset
                BodyVelocity.Velocity=Vector3.zero
                local currentPop=targetVehicle:GetAttribute("VehicleTiresLastPop")
                if currentPop and initialPop and currentPop~=initialPop then
                    print("[TIRES POPPED!]")
                    break
                end
                if not targetChar:GetAttribute("InVehicle") or not targetVehicle:IsDescendantOf(workspace) then
                    print("[TARGET EXITED VEHICLE] Breaking ram loop...")
                    break
                end
                task.wait()
            end
            task.wait(Config.CarLoopTpDuration)
        else
            local tStart=tick()
            while tick()-tStart<Config.CarLoopTpDuration and getgenv().AutoArrestActive and isTargetValid(targetRoot) do
                if CheckLagback(rootPart) then
                    BodyVelocity:Destroy()
                    BodyGyro:Destroy()
                    return
                end
                pcall(function()
                    sethiddenproperty(rootPart,"PhysicsRepRootPart",targetRoot)
                end)
                rootPart.CFrame=targetRoot.CFrame*Config.TpOffset
                BodyVelocity.Velocity=Vector3.zero
                task.wait()
            end
        end
    end
    BodyVelocity.Velocity=Vector3.zero
    lastCarExited=humanoid.SeatPart and humanoid.SeatPart.Parent or lastCarExited
    rootPart.AssemblyLinearVelocity=Vector3.zero
    rootPart.AssemblyAngularVelocity=Vector3.zero
    task.wait(0.1)
    BodyVelocity:Destroy()
    BodyGyro:Destroy()
    rootPart.AssemblyLinearVelocity=Vector3.zero
    rootPart.AssemblyAngularVelocity=Vector3.zero
    pcall(function()
        if ExitFunc then
            ExitFunc()
        end
        VirtualInputManager:SendKeyEvent(true,Enum.KeyCode.Space,false,game)
        task.wait(0.1)
        VirtualInputManager:SendKeyEvent(false,Enum.KeyCode.Space,false,game)
    end)
    if rootPart:FindFirstChild("ExecFlyAttachment") then
        rootPart.ExecFlyAttachment:Destroy()
    end
    if rootPart:FindFirstChild("ExecFlyVelocity") then
        rootPart.ExecFlyVelocity:Destroy()
    end
    local attachment=Instance.new("Attachment",rootPart)
    attachment.Name="ExecFlyAttachment"
    local linearVelocity=Instance.new("LinearVelocity",rootPart)
    linearVelocity.Name="ExecFlyVelocity"
    linearVelocity.Attachment0=attachment
    linearVelocity.RelativeTo=Enum.ActuatorRelativeTo.World
    linearVelocity.MaxForce=math.huge
    linearVelocity.VectorVelocity=Vector3.zero
    linearVelocity.Enabled=true
    if renderConnection then
        renderConnection:Disconnect()
    end
    local followLastPos=rootPart.Position
    local followStuckSince=tick()
    renderConnection=RunService.RenderStepped:Connect(function()
        if not getgenv().AutoArrestActive or not isTargetValid(targetRoot) then
            linearVelocity.VectorVelocity=Vector3.zero
            return
        end
        if CheckLagback(rootPart) then
            linearVelocity.VectorVelocity=Vector3.zero
            return
        end
        local moved=(rootPart.Position-followLastPos).Magnitude
        if moved>=Config.AntiStuckMinMove then
            followLastPos=rootPart.Position
            followStuckSince=tick()
        elseif tick()-followStuckSince>=Config.AntiStuckTimeout then
            linearVelocity.VectorVelocity=Vector3.zero
            ResetCharacter("anti-stuck while following target")
            return
        end
        local offset=targetRoot.Position-rootPart.Position
        local dist=offset.Magnitude
        if dist>Config.PlayerFlyStopDistance then
            local velocity=offset.Unit*Config.PlayerFlySpeed
            local _,peak,mountainFound,forwardDist=ScanTerrainAhead(rootPart,velocity)
            if mountainFound and forwardDist<250 then
                local desiredY=peak+20
                local yDiff=desiredY-rootPart.Position.Y
                local vy=math.clamp(yDiff*6,-350,170)
                if yDiff>1 then vy=math.clamp(yDiff*6,25,170) end
                if yDiff<-1 then vy=-math.clamp(math.abs(yDiff)*4,20,350) end
                velocity=Vector3.new(velocity.X,vy,velocity.Z)
            end
            linearVelocity.VectorVelocity=velocity
        else
            linearVelocity.VectorVelocity=Vector3.zero
        end
    end)
    local arrestLastPos=rootPart.Position
    local arrestStuckSince=tick()
    while getgenv().AutoArrestActive and isTargetValid(targetRoot) do
        local arrestMoved=(rootPart.Position-arrestLastPos).Magnitude
        if arrestMoved>=Config.AntiStuckMinMove then
            arrestLastPos=rootPart.Position
            arrestStuckSince=tick()
        elseif tick()-arrestStuckSince>=Config.AntiStuckTimeout then
            ResetCharacter("anti-stuck during arrest")
            return
        end
        if ReturnToLastExitedCar(rootPart,char,humanoid) then
            print("[FOLLOW SAFETY] Back at last car. Restarting target cycle from the vehicle.")
            return
        end
        if CheckLagback(rootPart) then
            break
        end
        local dist=(rootPart.Position-targetRoot.Position).Magnitude
        if dist<=Config.ArrestTpDistance then
            pcall(function()
                sethiddenproperty(rootPart,"PhysicsRepRootPart",targetRoot)
            end)
            if humanoid then
                humanoid.Sit=false
            end
            rootPart.CFrame=targetRoot.CFrame*CFrame.new(0,3,2)*Config.TpOffset
            EquipHandcuffs()
            task.wait(0.1)
            local targetChar=targetRoot.Parent
            local targetPlayerObj=Players:GetPlayerFromCharacter(targetChar)
            ExitAccidentalVehicle(humanoid)
            if targetChar:GetAttribute("InVehicle") then
                EjectNearestVehicleUser(targetRoot)
            else
                if attemptArrest and targetPlayerObj then
                    pcall(function()
                        attemptArrest(targetPlayerObj)
                    end)
                end
            end
            if targetChar:GetAttribute("HasHandcuffs") then
                print("[HANDCUFFED]",targetChar.Name)
                if targetPlayerObj then
                    arrestedBlacklist[targetPlayerObj]=true
                end
                break
            end
        end
        task.wait()
    end
    UnequipHandcuffs()
    if renderConnection then
        renderConnection:Disconnect()
        renderConnection=nil
    end
    if linearVelocity then
        linearVelocity:Destroy()
    end
    if attachment then
        attachment:Destroy()
    end
    task.wait(0.5)
end
ExecuteAutoArrest=function()
    if CheckServerHop() then
        return
    end
    getgenv().AutoArrestActive=true
    getgenv().LagbackCount=0
    phase1Complete=false
    phase1PathComplete=false
    phase1FinalPosition=nil
    resetQueued=false
    print("[AUTO ARREST] Starting execution - PHASE 1")
    local initChar=LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
    local initRoot=initChar:WaitForChild("HumanoidRootPart")
    local initHum=initChar:WaitForChild("Humanoid")
    print("[PHASE 1] Running destination path. No vehicle search or upward flight until it finishes.")
    while getgenv().AutoArrestActive and LocalPlayer.Character==initChar and not phase1Complete do
        if not GoToClosestStationAndGetCar(initRoot,initChar,initHum) then
            if not getgenv().AutoArrestActive or LocalPlayer.Character~=initChar then
                return
            end
            task.wait(1)
        end
    end
    if not phase1Complete or not getgenv().AutoArrestActive or LocalPlayer.Character~=initChar then
        return
    end
    print("[PHASE 1 COMPLETE] Vehicle secured. Auto-arrest cycle unlocked.")
    while getgenv().AutoArrestActive and LocalPlayer.Character==initChar do
        ExecuteAutoArrestCycle()
        task.wait()
    end
end

LocalPlayer.CharacterAdded:Connect(function(newChar)
    getgenv().AutoArrestActive=false
    getgenv().LagbackCount=0
    phase1Complete=false
    phase1FinalPosition=nil
    currentTarget=nil
    targetRoot=nil
    resetQueued=false
    if renderConnection then
        renderConnection:Disconnect()
        renderConnection=nil
    end
    if ServerHopActive then
        return
    end
    task.spawn(function()
        local humanoid=newChar:WaitForChild("Humanoid",10)
        local root=newChar:WaitForChild("HumanoidRootPart",10)
        if not humanoid or not root then return end
        task.wait(1)
        if LocalPlayer.Character~=newChar or ServerHopActive then return end
        print("[RESPAWN] Restarting from PHASE 1...")
        ExecuteAutoArrest()
    end)
end)
ExecuteAutoArrest()
