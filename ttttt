local Players = game:GetService("Players")
local RS = game:GetService("ReplicatedStorage")
local workspace = game:GetService("Workspace")
local lp = Players.LocalPlayer
getgenv().AutoArrestActive = true
local Config = { CarFlyHeight = 500, CarAscendSpeed = 100, CarMaxRamDuration = 5, TpOffset = CFrame.new(0, 0, 0) }
local arrestedBlacklist = {}
local attemptArrest, rememberedCar
local Modules = {
	Vehicle = require(RS:WaitForChild("Vehicle"):WaitForChild("VehicleUtils")),
	UI = require(RS:WaitForChild("Module"):WaitForChild("UI")),
	ItemSystem = require(RS:WaitForChild("Game"):WaitForChild("ItemSystem"):WaitForChild("ItemSystem"))
}
local PoliceData = {
	["Custom Main Station Path"] = { Base = Vector3.new(-1173.24,39.42,-1583.77), Path = { Vector3.new(-1173.24,39.42,-1583.77), Vector3.new(-1177.93,39.42,-1580.66), Vector3.new(-1184.32,39.42,-1575.63), Vector3.new(-1188.49,39.42,-1568.75), Vector3.new(-1187.33,39.42,-1560.60), Vector3.new(-1184.78,39.42,-1552.89), Vector3.new(-1182.62,39.42,-1546.72), Vector3.new(-1175.61,34.97,-1546.03), Vector3.new(-1168.76,30.16,-1546.55), Vector3.new(-1161.44,27.15,-1547.99), Vector3.new(-1156.07,23.25,-1551.89), Vector3.new(-1154.36,21.12,-1559.06), Vector3.new(-1157.26,19.07,-1566.29), Vector3.new(-1162.58,19.02,-1572.38), Vector3.new(-1169.74,19.02,-1576.19), Vector3.new(-1177.62,19.02,-1578.67), Vector3.new(-1185.61,19.07,-1580.14), Vector3.new(-1193.86,19.07,-1580.63), Vector3.new(-1201.85,19.07,-1580.43), Vector3.new(-1209.98,19.07,-1580.09), Vector3.new(-1218.11,19.07,-1580.18), Vector3.new(-1226.36,19.07,-1579.78), Vector3.new(-1234.43,19.02,-1578.75), Vector3.new(-1239.75,19.07,-1573.05), Vector3.new(-1243.11,19.07,-1565.64), Vector3.new(-1248.50,19.02,-1559.53), Vector3.new(-1256.50,19.02,-1558.65), Vector3.new(-1262.43,19.02,-1554.62), Vector3.new(-1259.15,18.71,-1549.22), Vector3.new(-1251.26,18.62,-1547.54), Vector3.new(-1244.88,18.62,-1546.87) } },
	["Fourth Station"] = { Base = Vector3.new(1786.66,24.05,-4019.01), Path = { Vector3.new(1762.26,24.00,-4024.93), Vector3.new(1765.20,24.02,-4026.29), Vector3.new(1772.61,24.05,-4029.64), Vector3.new(1779.17,24.00,-4034.16), Vector3.new(1785.04,24.00,-4040.16), Vector3.new(1790.33,24.25,-4046.51), Vector3.new(1792.76,24.50,-4049.68), Vector3.new(1793.25,24.50,-4053.24), Vector3.new(1790.91,24.09,-4061.18), Vector3.new(1786.38,24.10,-4068.25), Vector3.new(1784.77,24.10,-4076.59), Vector3.new(1781.65,23.80,-4084.53), Vector3.new(1778.85,23.80,-4092.15), Vector3.new(1778.73,23.80,-4093.07) } },
	["Main Police Station Ground"] = { Base = Vector3.new(-1171.10,18.80,-1579.22), Path = { Vector3.new(-1142.16,18.85,-1586.02), Vector3.new(-1146.80,18.85,-1585.55), Vector3.new(-1155.16,18.85,-1584.71), Vector3.new(-1163.53,18.85,-1583.90), Vector3.new(-1171.65,18.85,-1583.56), Vector3.new(-1179.64,18.85,-1583.26), Vector3.new(-1188.04,18.85,-1582.97), Vector3.new(-1196.30,18.85,-1582.68), Vector3.new(-1204.70,18.85,-1582.39), Vector3.new(-1212.83,18.85,-1582.11), Vector3.new(-1220.96,18.85,-1581.82), Vector3.new(-1229.09,18.85,-1581.42), Vector3.new(-1236.90,18.80,-1579.45), Vector3.new(-1241.89,18.85,-1573.22), Vector3.new(-1244.30,18.85,-1565.59), Vector3.new(-1249.61,18.80,-1559.48), Vector3.new(-1257.60,18.80,-1559.35), Vector3.new(-1263.00,18.79,-1555.37), Vector3.new(-1260.26,18.40,-1547.61), Vector3.new(-1252.41,18.40,-1546.17), Vector3.new(-1244.15,18.40,-1546.12), Vector3.new(-1244.08,18.40,-1546.12) } },
	["Museum Police Station"] = { Base = Vector3.new(738.20,44.96,1120.53), Path = { Vector3.new(732.75,44.96,1125.10), Vector3.new(733.10,44.96,1122.05), Vector3.new(733.02,44.96,1113.69), Vector3.new(731.74,44.96,1105.80), Vector3.new(730.05,44.96,1097.57), Vector3.new(728.25,44.95,1089.37), Vector3.new(730.33,44.94,1081.29), Vector3.new(735.58,44.37,1075.00), Vector3.new(741.75,44.77,1069.30), Vector3.new(744.70,44.96,1066.60) } },
	["Military Base"] = { Base = Vector3.new(1801.00,25.54,-891.02), Path = { Vector3.new(1805.17,25.54,-900.65), Vector3.new(1805.17,25.54,-900.65), Vector3.new(1799.38,25.54,-901.80), Vector3.new(1791.31,25.57,-902.75), Vector3.new(1783.10,25.60,-903.71), Vector3.new(1775.02,25.54,-904.70), Vector3.new(1766.97,25.54,-905.80), Vector3.new(1758.94,25.54,-907.13), Vector3.new(1753.12,25.54,-908.27), Vector3.new(1753.12,25.54,-908.27) } },
	["Fifth Station"] = { Base = Vector3.new(1822.10,25.55,-636.60), Path = { Vector3.new(1820.55,25.54,-607.59), Vector3.new(1822.75,25.54,-616.20), Vector3.new(1827.02,25.54,-623.28), Vector3.new(1831.25,25.54,-630.38), Vector3.new(1835.55,25.08,-637.60), Vector3.new(1844.01,25.00,-651.80), Vector3.new(1848.31,25.00,-659.02) } }
}
for _, v in pairs(getgc(true)) do
	if type(v) == "function" and islclosure(v) then
		pcall(function() if tostring(getinfo(v).name) == "AttemptArrest" then attemptArrest = v end end)
		if attemptArrest then break end
	end
end
local function monitor(p)
	local function char(c)
		c:GetAttributeChangedSignal("HasHandcuffs"):Connect(function()
			if c:GetAttribute("HasHandcuffs") then arrestedBlacklist[p] = true end
		end)
	end
	if p.Character then char(p.Character) end
	p.CharacterAdded:Connect(char)
end
for _, p in ipairs(Players:GetPlayers()) do monitor(p) end
Players.PlayerAdded:Connect(monitor)
task.spawn(function()
	while task.wait(0.5) do
		local ok, m = pcall(Modules.Vehicle.GetLocalVehicleModel)
		if ok and m and m ~= "" and m ~= false then
			local car = typeof(m) == "Instance" and m or workspace:FindFirstChild(m, true)
			if car and rememberedCar ~= car then rememberedCar = car end
		end
	end
end)
local function checkObstacle(root, dirVector)
	local params = RaycastParams.new()
	params.FilterDescendantsInstances = {lp.Character, workspace:FindFirstChild("Vehicles")}
	params.FilterType = Enum.RaycastFilterType.Exclude
	local result = workspace:Raycast(root.Position, dirVector, params)
	if result and result.Distance < 5 then
		local hum = lp.Character:FindFirstChild("Humanoid")
		if hum then hum.Health = 0 end
	end
end
local function EquipHandcuffs()
	local invSys = require(RS.Inventory.InventoryItemSystem)
	for _, item in pairs(invSys.getInventoryItemsFor(lp)) do
		if item.obj and item.obj.Name == "Handcuffs" then
			pcall(function() item:AttemptSetEquipped(true) end)
			return true
		end
	end
	return false
end
local function EjectNearestVehicleUser()
	local hrp = lp.Character and lp.Character:FindFirstChild("HumanoidRootPart")
	local vehicles = workspace:FindFirstChild("Vehicles")
	if not hrp or not vehicles then return end
	local nearest, minDist = nil, 25
	for _, v in pairs(vehicles:GetChildren()) do
		local seat = v:FindFirstChild("Seat") or v:FindFirstChild("VehicleSeat") or v:FindFirstChildWhichIsA("BasePart", true)
		if seat then
			local d = (hrp.Position - seat.Position).Magnitude
			if d < minDist then minDist, nearest = d, v end
		end
	end
	if not nearest then return end
	if Modules.UI and Modules.UI.CircleAction and Modules.UI.CircleAction.Specs then
		for _, spec in pairs(Modules.UI.CircleAction.Specs) do
			if (spec.Part and (nearest:IsAncestorOf(spec.Part) or spec.Part:IsDescendantOf(nearest))) or (spec.Text and (spec.Text:lower():find("eject") or spec.Text:lower():find("passenger"))) then
				pcall(function() spec:Callback(true) end)
				return
			end
		end
	end
end
local function isTargetValid(root)
	if not root or not root.Parent then return false end
	local plr = Players:GetPlayerFromCharacter(root.Parent)
	if not plr or arrestedBlacklist[plr] then return false end
	if plr.Team and (plr.Team.Name == "Criminal" or plr.Team.Name == "Prisoner") then
		if plr.Character:GetAttribute("InVehicle") then
			local vehiclesDir = workspace:FindFirstChild("Vehicles")
			if vehiclesDir then
				for _, v in pairs(vehiclesDir:GetChildren()) do
					local seat = v:FindFirstChild("Seat") or v:FindFirstChild("VehicleSeat") or v:FindFirstChildWhichIsA("BasePart", true)
					if seat and (seat.Position - root.Position).Magnitude < 15 then
						local vName = v.Name:lower()
						if vName:find("heli") or vName:find("drone") or vName:find("blimp") then return false end
					end
				end
			end
		end
		return true
	end
	return false
end
local function getClosestCriminal(rootPart)
	local closestRoot, minDist = nil, math.huge
	for _, plr in pairs(Players:GetPlayers()) do
		if plr ~= lp and plr.Character then
			local tRoot = plr.Character:FindFirstChild("HumanoidRootPart")
			if isTargetValid(tRoot) then
				local dist = (tRoot.Position - rootPart.Position).Magnitude
				if dist < minDist then minDist = dist; closestRoot = tRoot end
			end
		end
	end
	return closestRoot
end
local function escapeBuilding(rootPart)
	local closestPath, minDist = nil, math.huge
	for _, data in pairs(PoliceData) do
		local dist = (rootPart.Position - data.Base).Magnitude
		if dist < minDist then minDist = dist; closestPath = data.Path end
	end
	if minDist < 100 and closestPath then
		for _, wp in ipairs(closestPath) do
			if lp.Character and lp.Character:FindFirstChild("HumanoidRootPart") then
				lp.Character.HumanoidRootPart.CFrame = CFrame.new(wp)
				task.wait(0.2)
			end
		end
	end
end
local function enterTargetVehicle(vehicle)
	local seat = vehicle:FindFirstChild("Seat") or vehicle:FindFirstChild("VehicleSeat")
	if not seat then return end
	if Modules.UI and Modules.UI.CircleAction and Modules.UI.CircleAction.Specs then
		for _, spec in pairs(Modules.UI.CircleAction.Specs) do
			if spec.Part and spec.Part == seat then
				pcall(function() spec:Callback(true) end)
				break
			end
		end
	end
end
local function spawnAndEnterCamaro(rootPart)
	local params = RaycastParams.new()
	params.FilterDescendantsInstances = {lp.Character, workspace:FindFirstChild("Vehicles")}
	params.FilterType = Enum.RaycastFilterType.Exclude
	local result = workspace:Raycast(rootPart.Position, Vector3.new(0, -9999, 0), params)
	if result then rootPart.CFrame = CFrame.new(result.Position + Vector3.new(0, 5, 0)) end
	task.wait(0.5)
	local spawnRemote = RS:FindFirstChild("GarageSpawnVehicle", true)
	if spawnRemote then spawnRemote:FireServer("Chassis", "Camaro") end
	task.wait(1)
	if rememberedCar then enterTargetVehicle(rememberedCar) end
end
local function flyToTarget(rootPart, targetPos, isVehicleDescend)
	local BodyGyro = Instance.new("BodyGyro", rootPart)
	local BodyVelocity = Instance.new("BodyVelocity", rootPart)
	BodyVelocity.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
	BodyGyro.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
	BodyGyro.D = 500; BodyGyro.P = 150000
	local reachedHoriz = false
	while getgenv().AutoArrestActive and lp.Character and lp.Character:FindFirstChild("Humanoid") and lp.Character.Humanoid.Health > 0 do
		BodyGyro.CFrame = CFrame.new(rootPart.Position, rootPart.Position + Vector3.new(0, 0, -1))
		local flatTarget = Vector3.new(targetPos.X, rootPart.Position.Y, targetPos.Z)
		local hDist = (flatTarget - rootPart.Position).Magnitude
		if not reachedHoriz then
			if hDist > 10 then
				BodyVelocity.Velocity = (flatTarget - rootPart.Position).Unit * Config.CarAscendSpeed
			else
				reachedHoriz = true
			end
		else
			local vDist = (targetPos - rootPart.Position).Magnitude
			if vDist > (isVehicleDescend and 15 or 25) then
				BodyVelocity.Velocity = (targetPos - rootPart.Position).Unit * Config.CarAscendSpeed
				checkObstacle(rootPart, Vector3.new(0, -5, 0))
			else
				break
			end
		end
		task.wait()
	end
	BodyVelocity:Destroy(); BodyGyro:Destroy()
end
task.spawn(function()
	while getgenv().AutoArrestActive do
		task.wait(0.1)
		local char = lp.Character
		local root = char and char:FindFirstChild("HumanoidRootPart")
		local hum = char and char:FindFirstChild("Humanoid")
		if not root or not hum or hum.Health <= 0 then continue end
		escapeBuilding(root)
		local inVehicle = char:GetAttribute("InVehicle") or (hum.SeatPart ~= nil)
		if not inVehicle then
			local vDir = workspace:FindFirstChild("Vehicles")
			local nCar, nDist = nil, math.huge
			if vDir then
				for _, v in pairs(vDir:GetChildren()) do
					local seat = v:FindFirstChild("Seat") or v:FindFirstChild("VehicleSeat")
					if seat then
						local d = (root.Position - seat.Position).Magnitude
						if d < nDist then nDist = d; nCar = v end
					end
				end
			end
			if nCar and nDist < 200 then
				flyToTarget(root, nCar.PrimaryPart and nCar.PrimaryPart.Position or nCar:GetModelCFrame().Position, true)
				enterTargetVehicle(nCar)
			else
				spawnAndEnterCamaro(root)
			end
			task.wait(1)
			inVehicle = char:GetAttribute("InVehicle") or (hum.SeatPart ~= nil)
		end
		if inVehicle then
			local BodyGyro = Instance.new("BodyGyro", root)
			local BodyVelocity = Instance.new("BodyVelocity", root)
			BodyVelocity.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
			BodyGyro.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
			BodyGyro.D = 500; BodyGyro.P = 150000
			local targetRoot = nil
			while getgenv().AutoArrestActive and hum.Health > 0 do
				targetRoot = getClosestCriminal(root)
				if targetRoot then break end
				if root.Position.Y < Config.CarFlyHeight then
					BodyVelocity.Velocity = Vector3.new(0, Config.CarAscendSpeed, 0)
					checkObstacle(root, Vector3.new(0, 5, 0))
				else
					BodyVelocity.Velocity = Vector3.zero
				end
				BodyGyro.CFrame = CFrame.new(root.Position, root.Position + Vector3.new(0, 0, -1))
				task.wait()
			end
			BodyVelocity:Destroy(); BodyGyro:Destroy()
			if targetRoot then
				flyToTarget(root, targetRoot.Position + Vector3.new(0, 100, 0), false)
				flyToTarget(root, targetRoot.Position, false)
				if (root.Position - targetRoot.Position).Magnitude < 30 then
					local tChar = targetRoot.Parent
					if tChar and tChar:GetAttribute("InVehicle") then
						local tVeh = tChar.Humanoid.SeatPart and tChar.Humanoid.SeatPart.Parent
						if tVeh then
							local initialPop = tVeh:GetAttribute("VehicleTiresLastPop")
							local rStart = tick()
							local popBodyVelocity = Instance.new("BodyVelocity", root)
							popBodyVelocity.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
							while getgenv().AutoArrestActive and isTargetValid(targetRoot) and (tick() - rStart < Config.CarMaxRamDuration) do
								pcall(function() sethiddenproperty(root, "PhysicsRepRootPart", targetRoot) end)
								root.CFrame = targetRoot.CFrame * Config.TpOffset
								popBodyVelocity.Velocity = Vector3.zero
								if tVeh:GetAttribute("VehicleTiresLastPop") ~= initialPop then break end
								task.wait()
							end
							popBodyVelocity:Destroy()
						end
					end
					task.wait(0.1)
					if char:GetAttribute("InVehicle") then
						pcall(function() require(RS.Module.UI).CircleAction.Specs[1]:Callback(true) end)
						task.wait(0.5)
					end
					EquipHandcuffs()
					local attempts = 0
					while getgenv().AutoArrestActive and isTargetValid(targetRoot) and not tChar:GetAttribute("HasHandcuffs") and attempts < 20 do
						pcall(function() sethiddenproperty(root, "PhysicsRepRootPart", targetRoot) end)
						root.CFrame = targetRoot.CFrame * Config.TpOffset
						if tChar:GetAttribute("InVehicle") then EjectNearestVehicleUser() else if attemptArrest then pcall(function() attemptArrest(Players:GetPlayerFromCharacter(tChar)) end) end end
						attempts = attempts + 1
						task.wait(0.2)
					end
					if tChar:GetAttribute("HasHandcuffs") then arrestedBlacklist[Players:GetPlayerFromCharacter(tChar)] = true end
					local upPos = root.Position + Vector3.new(0, 50, 0)
					root.CFrame = CFrame.new(upPos)
					task.wait(0.2)
					if rememberedCar and rememberedCar.PrimaryPart and (rememberedCar.PrimaryPart.Position - root.Position).Magnitude < 500 then
						flyToTarget(root, rememberedCar.PrimaryPart.Position, true)
						enterTargetVehicle(rememberedCar)
					else
						root.CFrame = CFrame.new(root.Position + Vector3.new(300, 0, 0))
						spawnAndEnterCamaro(root)
					end
				end
			end
		end
	end
end)
